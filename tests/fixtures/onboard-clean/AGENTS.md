# Agents

This is a consumer project's AGENTS.md before any SPADE onboarding has run.
It carries the project's own rules and will receive a marker-wrapped SPADE
section when `spade-marker-replace` is invoked against it.

## Project-specific rules

- Prefer explicit types over `any`.
- Every pull request must have tests.
- No emoji in source files.
